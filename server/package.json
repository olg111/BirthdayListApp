{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate": "node ./index.js > ./db.json",
    "generate-dry-run": "node ./index.js",
    "server": "json-server -p 4000 --watch ./db.json"    
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.3"
  },
  "devDependencies": {
    "@faker-js/faker": "^8.0.2"
  }
}
